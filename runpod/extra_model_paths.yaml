# ComfyUI uses this file to discover model directories outside its install
# tree. The Network Volume is mounted at /workspace on this Serverless
# endpoint (matching the path we used during bootstrap on the Pod).

runpod_volume:
  base_path: /workspace/ComfyUI/
  is_default: true
  diffusion_models: models/diffusion_models/
  text_encoders: models/text_encoders/
  vae: models/vae/
  loras: models/loras/
  upscale_models: models/upscale_models/
  clip_vision: models/clip_vision/
  controlnet: models/controlnet/
